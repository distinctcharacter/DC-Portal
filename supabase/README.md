# Legacy Supabase Archive

This directory is retained only as a historical record of the portal's former database design.

The production portal uses Clerk authentication and Neon Postgres. Do not run these SQL files against Neon, do not add Supabase environment variables to Netlify, and do not import Supabase clients into active application code.

Current database files are under `neon/`.

